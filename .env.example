# Django Settings
DJANGO_SECRET_KEY="your_very_secret_random_key_here"
DJANGO_DEBUG="True" # Set to "False" in production
DJANGO_ALLOWED_HOSTS="localhost,127.0.0.1" # Comma-separated list of allowed hosts

# Database Settings (PostgreSQL example)
# DATABASE_URL="postgres://user:password@host:port/dbname"
# Example for local PostgreSQL:
DATABASE_URL="postgres://darziuser:darzipass@localhost:5432/darziflow_db"
# For SQLite (default if DATABASE_URL is not set, but not recommended for production):
# DATABASE_URL="sqlite:///./db.sqlite3"

# CORS Settings
CORS_ALLOWED_ORIGINS="http://localhost:3000,http://127.0.0.1:3000" # Frontend URL(s)

# JWT Settings (optional, defaults are in settings.py)
# ACCESS_TOKEN_LIFETIME_MINUTES=60
# REFRESH_TOKEN_LIFETIME_DAYS=1

# Email Settings (if email functionality is added later)
# EMAIL_BACKEND='django.core.mail.backends.smtp.EmailBackend'
# EMAIL_HOST=''
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=''
# EMAIL_HOST_PASSWORD=''
# DEFAULT_FROM_EMAIL=''
